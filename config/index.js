const app = require('./app');
const express = require('./express');
const routes = require('./routes');
const dbConfig = require('./dbConfig');
module.exports = {
    app, express, routes, dbConfig
};
